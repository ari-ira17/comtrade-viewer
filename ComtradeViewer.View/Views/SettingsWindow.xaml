<Window x:Class="ComtradeViewer.View.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:ComtradeViewer.ViewModel.Models;assembly=ComtradeViewer.ViewModel"
        xmlns:vm="clr-namespace:ComtradeViewer.ViewModel.ViewModels;assembly=ComtradeViewer.ViewModel"
        xmlns:local="clr-namespace:ComtradeViewer.View.Views"
        Title="Настройки отображения" Width="400" Height="500"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize">
    <Window.Resources>
        <ObjectDataProvider x:Key="TimeFormats" MethodName="GetValues"
                ObjectType="{x:Type viewModels:TimeFormat}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="viewModels:TimeFormat"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <Style x:Key="ColorPickerComboBox" TargetType="ComboBox">
            <Setter Property="ItemsSource" Value="{x:Static vm:ChannelVisibilityItem.ColorPaletteColors}"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Background="{Binding SelectedItem, RelativeSource={RelativeSource TemplatedParent}, Converter={x:Static local:SettingsWindow.ColorToBrushConverter}}"
                                          BorderBrush="Gray" BorderThickness="1"
                                          Width="30" Height="20"/>
                            <Popup x:Name="Popup" 
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom" StaysOpen="False">
                                <Border Background="White" BorderBrush="Gray" BorderThickness="1">
                                    <ItemsPresenter/>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="2">
                            <Rectangle Width="20" Height="12" Fill="{Binding Converter={x:Static local:SettingsWindow.ColorToBrushConverter}}"/>
                            <TextBlock Text="{Binding}" Margin="5,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Формат времени:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox Width="150" ItemsSource="{Binding Source={StaticResource TimeFormats}}"
                          SelectedItem="{Binding AppSettings.SelectedTimeFormat}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={x:Static local:SettingsWindow.TimeFormatConverter}}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>
        </StackPanel>

        <ListBox Grid.Row="1" ItemsSource="{Binding SettingsChannels}"
                 HorizontalContentAlignment="Stretch" ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="3,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <CheckBox Grid.Column="0" IsChecked="{Binding IsVisible}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Grid.Column="1" Text="{Binding ChannelName}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                        <ComboBox Grid.Column="2" Style="{StaticResource ColorPickerComboBox}"
                                  SelectedItem="{Binding SelectedColor, Mode=TwoWay}" 
                                  Width="40" Height="22"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="ОК" Width="80" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="Отмена" Width="80" IsCancel="True" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
